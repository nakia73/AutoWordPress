# Stream 02: WordPress Setup - 単体開発仕様

> **上位ドキュメント:** [開発哲学](../DEVELOPMENT_PHILOSOPHY.md)
>
> **サービス名:** Argo Note
> **スコープ:** 単体開発（スタンドアローン）
> **最終更新:** 2026-01-30
>
> **関連ドキュメント:**
> - [Stream02_Tasks.md](./Stream02_Tasks.md) - タスク管理表
> - [Stream02_WordPress.md](./Stream02_WordPress.md) - 技術詳細・将来拡張

---

## 1. Stream02のテスト目的

**WordPressがセットアップでき、記事投稿機能が動作するか** を検証する。

ユーザーがUIから簡単な入力をするだけで、独自のサーバー上にWordPress環境が構築され、記事が投稿できることを確認する。

### 1.1 テスト対象（Stream02の責務）

| ステップ | 内容 | 状態 |
|---------|------|------|
| 1 | VPSプロビジョニング（Hetzner API） | 📋 計画 |
| 2 | OS基盤設定（Nginx, PHP-FPM, MariaDB） | 📋 計画 |
| 3 | WordPressインストール | 📋 計画 |
| 4 | Multisite有効化 | 📋 計画 |
| 5 | サブサイト作成（WP-CLI） | 📋 計画 |
| 6 | 認証情報発行（Application Password） | 📋 計画 |
| 7 | **記事投稿機能の動作確認（Mockデータ使用）** | 📋 計画 |

### 1.2 記事投稿テストについて

Stream02では記事の「内容」ではなく「投稿機能」をテストする。

| 項目 | Stream02 | Stream04 |
|------|---------|---------|
| テスト対象 | 投稿機能が動作するか | Stream01との連携が動作するか |
| 使用データ | **Mockデータ**（固定テキスト・画像） | **Stream01の出力**（実記事） |
| 目的 | WordPress REST APIの動作検証 | 生成〜投稿の結合検証 |

```typescript
// Stream02でのテスト例
import { MOCK_ARTICLE, createMockArticleWithImage } from '@/lib/wordpress/mock-data';

// Mockデータで投稿テスト
await articlePublisher.publish(MOCK_ARTICLE);
```

### 1.3 テスト対象外（他Streamの責務）

| 機能 | 担当Stream | 理由 |
|------|-----------|------|
| 記事生成 | Stream01 | コンテンツ生成は別責務 |
| Stream01出力を使った投稿 | Stream04 | Stream01とStream02の結合処理 |
| ユーザー認証 | Stream03 | 認証は別責務 |

---

## 2. 入出力仕様

### 2.1 入力

```typescript
interface SetupWordPressInput {
  // ユーザー情報
  userId: string;
  email: string;

  // サイト設定
  siteSlug: string;      // サブドメイン（例: "user-123"）
  siteTitle: string;     // サイトタイトル

  // オプション
  theme?: string;        // テーマ名（デフォルト: 標準テーマ）
}
```

### 2.2 出力

```typescript
interface SetupWordPressOutput {
  success: boolean;
  data?: {
    // サイト情報
    siteId: number;
    url: string;              // https://user-123.argonote.app
    adminUrl: string;         // https://user-123.argonote.app/wp-admin
    restApiUrl: string;       // https://user-123.argonote.app/wp-json/wp/v2

    // 認証情報（Stream04での記事投稿に使用）
    credentials: {
      username: string;
      applicationPassword: string;  // AES-256-GCM暗号化推奨
    };

    createdAt: string;
  };
  error?: {
    code: 'VPS_ERROR' | 'INSTALL_ERROR' | 'SITE_EXISTS' | 'WP_CLI_ERROR' | 'SSH_ERROR' | 'UNKNOWN';
    message: string;
  };
}
```

---

## 3. コンポーネント設計

### 3.1 コンポーネント一覧

```
┌─────────────────────────────────────────────────────────────────┐
│                   Stream 02: WordPress Setup                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [Public Interface]                                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  WordPressSetupManager                                  │   │
│  │  - setupSite(input): SetupWordPressOutput               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  [Internal Services]                                            │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐ │
│  │  VPSProvisioner  │  │  SiteManager     │  │  Article     │ │
│  │  - provision()   │  │  - createSite()  │  │  Publisher   │ │
│  │  - configure()   │  │  - issueAppPwd() │  │  - publish() │ │
│  └──────────────────┘  └──────────────────┘  └──────────────┘ │
│           │                     │                    │         │
│           ▼                     ▼                    ▼         │
│  [Infrastructure Layer]                                         │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐ │
│  │  HetznerClient   │  │  WPCLIClient     │  │  WPClient    │ │
│  │  (Cloud API)     │  │  (SSH経由)       │  │  (REST API)  │ │
│  └──────────────────┘  └──────────────────┘  └──────────────┘ │
│           │                     │                    │         │
│           ▼                     ▼                    ▼         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Hetzner VPS + WordPress Multisite          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  [Test Data]                                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  mock-data.ts: MOCK_ARTICLE, createMockArticleWithImage │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 責務定義

| コンポーネント | 責務 | 依存先 |
|---------------|------|--------|
| **WordPressSetupManager** | 外部公開API。セットアップ全体を統括 | VPSProvisioner, SiteManager |
| **VPSProvisioner** | VPS作成・OS設定・WordPress基盤構築 | HetznerClient, SSHClient |
| **SiteManager** | サブサイト作成・認証情報発行 | WPCLIClient |
| **ArticlePublisher** | 記事投稿機能（Mockデータでテスト） | WordPressClient |
| **HetznerClient** | Hetzner Cloud API呼び出し | - |
| **WPCLIClient** | SSH経由でWP-CLIコマンドを実行 | SSHClient |
| **WordPressClient** | WordPress REST API呼び出し | - |
| **SSHClient** | SSH接続管理 | - |
| **mock-data.ts** | テスト用Mockデータ定義 | - |

---

## 4. ディレクトリ構成

[開発哲学](../DEVELOPMENT_PHILOSOPHY.md) に従い、`stream-02/` 内でスタンドアローン開発を行う。

```
stream-02/                          # スタンドアローン開発ディレクトリ
├── package.json
├── tsconfig.json
├── .env.example
├── README.md
├── src/
│   ├── app/                        # Stub UI（単体テスト用・統合しない）
│   │   ├── page.tsx                # ダッシュボード
│   │   ├── layout.tsx
│   │   ├── globals.css
│   │   ├── connection/             # 接続テスト
│   │   │   ├── page.tsx
│   │   │   └── actions.ts
│   │   ├── site/                   # サイト作成テスト
│   │   │   ├── page.tsx
│   │   │   └── actions.ts
│   │   └── article/                # 記事投稿テスト（Mockデータ使用）
│   │       ├── page.tsx
│   │       └── actions.ts
│   │
│   └── lib/                        # ★コアロジック（統合対象）
│       ├── wordpress/              # WordPressモジュール
│       │   ├── wordpress-setup-manager.ts  # 公開API（セットアップ）
│       │   ├── site-manager.ts             # サイト作成ロジック
│       │   ├── article-publisher.ts        # 記事投稿ロジック
│       │   ├── mock-data.ts                # テスト用Mockデータ
│       │   ├── client.ts                   # REST APIクライアント
│       │   ├── types.ts
│       │   ├── index.ts
│       │   └── __tests__/
│       │
│       ├── vps/                    # VPS管理モジュール
│       │   ├── ssh-client.ts           # SSH接続
│       │   ├── wp-cli.ts               # WP-CLI実行
│       │   ├── hetzner-client.ts       # Hetzner API（将来）
│       │   ├── provisioner.ts          # プロビジョニング（将来）
│       │   └── index.ts
│       │
│       └── crypto.ts               # 暗号化ユーティリティ
│
└── vitest.config.ts                # テスト設定

# 統合後（参考）
app/src/lib/wordpress/              # ← stream-02/src/lib/wordpress/ を統合
app/src/lib/vps/                    # ← stream-02/src/lib/vps/ を統合
```

**配置ルール:**
- **コアロジック** → `stream-02/src/lib/` に配置（統合対象）
- **Stub UI** → `stream-02/src/app/` に配置（統合しない）
- **統合時** → `src/lib/` 配下のみを `app/src/lib/` にコピー

---

## 5. セキュリティ

### 5.1 認証情報の保護

| 対策 | 実装 |
|------|------|
| 暗号化保存 | AES-256-GCM（`crypto.ts`） |
| 通信暗号化 | HTTPS必須（Cloudflare SSL） |
| 最小権限 | サイト単位でApplication Passwordを発行 |

---

## 6. Stub UI 仕様

### 6.1 目的

| 目的 | 説明 |
|------|------|
| 単体動作確認 | WordPressセットアップ・記事投稿の各ステップを個別にテスト |
| デバッグ | エラー時の原因特定 |

### 6.2 画面構成

| 画面 | URL | 機能 |
|------|-----|------|
| Dashboard | `/` | 各テスト画面へのナビゲーション、環境変数表示 |
| Connection | `/connection` | SSH/WP-CLI接続確認 |
| Site | `/site` | サイト作成テスト（セットアップ全体） |
| Article | `/article` | 記事投稿テスト（**Mockデータ使用**） |

**重要:** Article画面ではMockデータを使用してテストを行う。Stream01の出力は使用しない。

---

## 7. 環境変数

```bash
# .env.local

# Hetzner Cloud API（将来）
HETZNER_API_TOKEN="your-hetzner-api-token"

# VPS接続
VPS_HOST="xxx.xxx.xxx.xxx"
VPS_SSH_USER="root"
VPS_SSH_PRIVATE_KEY="base64-encoded-key"

# WordPress
WP_DOMAIN="argonote.app"
WP_PATH="/var/www/argonote"

# 暗号化
ENCRYPTION_KEY="64-char-hex-string"
```

---

## 8. 成功基準

| 項目 | 基準 |
|------|------|
| VPS作成 | 5分以内に完了（将来実装時） |
| WordPress基盤構築 | 10分以内に完了（将来実装時） |
| サブサイト作成 | 60秒以内に完了 |
| Application Password発行 | 即時 |
| 記事投稿（Mockデータ） | 10秒以内に完了 |
| 画像アップロード | 10秒以内に完了 |
| Stub UI | 全画面で正常動作 |

---

## 9. 開発フェーズ

### Phase 1: 基盤（現在）

サブサイト作成・認証情報発行・記事投稿機能の検証

- SiteManager実装
- WPCLIClient実装
- ArticlePublisher実装
- Mockデータ定義
- Stub UI（Connection, Site, Article画面）

### Phase 2: 自動プロビジョニング（将来）

VPS作成からWordPress構築までの完全自動化

- HetznerClient実装
- VPSProvisioner実装
- cloud-init設定
- WordPress自動インストール

---

*最終更新: 2026-01-30*
