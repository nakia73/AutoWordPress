# Stream 16 - UIコンポーネント開発

## スコープ制限

現在のStreamは単体のUIコンポーネント開発用です。Claudeの操作は **`/Users/apple/Dev/Autoblog/stream-13` ディレクトリのみ** に制限されます。
他所での変更が必要な場合はフォルダ内にドキュメントを作成しタスクとしてまとめてください。

### 許可される操作

- `stream-16/` 内でのファイル作成・編集・削除
- 他のStreamのファイルは参照のみ可
- stream-16用のビルド・テストコマンドの実行
  AGANTS.mdを読め

### 禁止される操作

- `stream-16/` 以外のファイル変更
- 他のStreamディレクトリへの新規ファイル作成
- プロジェクトレベルの設定変更
