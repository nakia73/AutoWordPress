import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Stream A: Standalone Article Generation Engine
  // No external dependencies (Supabase, WordPress, etc.)
};

export default nextConfig;
